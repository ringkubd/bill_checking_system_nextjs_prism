const channelList: {private: string[], public: string[], presence: string[]} = {
    private: [
        'v1/chat/send'
    ],
    public: [
        'v1/chat/send'
    ],
    presence: [
        'v1/chat/send'
    ],
}

export default channelList;
